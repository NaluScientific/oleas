import setuptools


setuptools.setup(
    version="0.1.1",
    name="oleas",
    description="OLEAS readout",
    python_requires=">=3.9",
    install_requires=[],
    packages=setuptools.find_packages(),
)
