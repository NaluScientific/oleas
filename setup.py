import os
import setuptools
from setuptools import setup

setup(
    name="oleas",
    description="OLEAS readout",
    python_requires=">=3.9",
    install_requires=[],
    packages=setuptools.find_packages(),
)
